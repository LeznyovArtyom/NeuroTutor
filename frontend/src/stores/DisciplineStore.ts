import { ref, computed } from 'vue'
import { defineStore } from 'pinia'

// export const useDisciplineStore = defineStore('discipline', {
//   actions: {
//     renameLocal(id:number|string, name:string) {
//       const d = this.list.find(d => d.id === Number(id))
//       if (d) d.name = name
//     }
//   }
// })
